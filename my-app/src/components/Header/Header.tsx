import React from "react";
import { HeaderTitle, HeaderWrap } from "./HeaderStyle";

const Header = () => {
  return (
    <HeaderWrap>
      <HeaderTitle>
        My ToDo List
      </HeaderTitle>
    </HeaderWrap>
  )
};

export default Header;
